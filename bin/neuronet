#!/usr/bin/env ruby
# frozen_string_literal: true

require 'help_parser'
require 'neuronet'

HelpParser[Neuronet::VERSION, <<~HELP]
  ## Neuronet ##
  Usage:
    neuronet [:options+]
  Options:
    -h --help
    -v --version
HELP
